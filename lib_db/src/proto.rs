// Include generated types for package ingest.imessage. Prost puts the message
// directly into this module when the package path matches the file stem.
include!(concat!(env!("OUT_DIR"), "/ingest.imessage.rs"));

// The generated type `ImessageRecord` is now available directly as
// `crate::proto::ImessageRecord`.
